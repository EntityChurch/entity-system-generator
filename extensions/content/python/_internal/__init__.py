"""Module-private to `entity_content`. Nothing here is part of the public surface.

Python cannot enforce that — see `reassemble.py`'s header for what the boundary actually
is in this language and why it is weaker than the `typescript` port's `exports` map.
"""
